docker compose up -d pgadmin

-------------------


docker compose up -d --build auth-service


-------------

maven

# Core model
Lifecycle → Phase → Plugin → Goal

# Common lifecycles
default  = build
clean    = cleanup
site     = docs

# Key phases (default lifecycle)
compile
test
package
install
deploy

# Run a phase (runs all previous phases)
mvn package
mvn install

# Run a specific goal
mvn spring-boot:run
mvn compiler:compile

# See why something runs
mvn -X package

# Inspect plugin goals
mvn help:describe -Dplugin=org.apache.maven.plugins:maven-compiler-plugin -Ddetail

# Inspect phase bindings
mvn help:describe -Dcmd=package -Ddetail

# See full resolved configuration
mvn help:effective-pom

# Dependency debugging
mvn dependency:tree

# Skip tests>
mvn package -DskipTests
mvn package -Dmaven.test.skip=true

# Use wrapper (recommended)
./mvnw package


./mvnw buildplan:list-phase -Dphase=compile

./mvnw help:describe -Dcmd=install

./mvnw verify -DskipTests=true -DskipITs=false


------------

Remove buggy nul file:


rename \\.\D:\CodingProjects\OnlineShop-claude\nul deletefile.txt 