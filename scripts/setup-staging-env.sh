#!/usr/bin/env bash
set -euo pipefail

echo "setup-staging-env.sh is retired." >&2
echo "Use scripts/resume-staging.sh for a deterministic clean deployment." >&2
echo "Use scripts/pause-staging.sh for teardown without snapshot retention." >&2
exit 1
