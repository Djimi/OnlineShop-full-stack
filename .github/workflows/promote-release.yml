name: Promote Release to Production (Retired)

# RETIRED during the greenfield live acceptance (Phase 6, 2026-08-17).
#
# The production-mutation path that lived here (the legacy finalize + SemVer
# promotion scripts) was neutralized to eliminate the OP-CUT-01
# concurrent-mutation risk with the greenfield pipeline. The greenfield
# equivalent is promote-release-greenfield.yml (candidate-run promotion
# behind the protected `production` Environment).
#
# This file is intentionally inert: no triggers, no jobs, no AWS access. It is
# kept in git for revert-recoverability (AD-18) and deleted at Phase 13.
# Restoring the legacy path is `git revert` of the Phase 6 neutralization —
# never edit this file back into a mutation workflow without owner approval.

permissions:
  contents: read