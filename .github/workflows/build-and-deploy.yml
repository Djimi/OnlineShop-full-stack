name: CI/CD Pipeline (Retired)

# RETIRED during the greenfield live acceptance (Phase 7, 2026-08-18).
#
# The application-delivery path that lived here (push-triggered builds,
# e2e-staging, candidate-evidence) was neutralized at the trigger swap. The
# greenfield equivalent is ci.yml: exactly one producer per sha
# (OP-CUT-01) — it owns push builds for main and feature/** and emits
# validated candidates with immutable sha-<fullsha> ECR tags.
#
# This file is intentionally inert: no triggers, no jobs, no AWS access. It is
# kept in git for revert-recoverability (AD-18) and deleted at Phase 13.
# Restoring the legacy path is `git revert` of the Phase 7 swap — never edit
# this file back into a build/deploy workflow without owner approval.

permissions:
  contents: read
