# Python dependencies for the release contract validator and candidate
# evidence gates (Pass 3, subphase 3.1 + 3.2). Versions are pinned so output
# normalization stays reproducible.
jsonschema==4.26.0
# Used by tests/scripts/candidate_evidence_test.sh to statically validate the
# CI workflow YAML (staging serialization, teardown ownership, OCI labels,
# SHA-pinned Actions). Test-only; not imported by the validator runtime.
PyYAML==6.0.3
